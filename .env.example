# Copy to .env and fill in. See README for where to get each value.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"
TMDB_READ_TOKEN="your TMDB API Read Access Token (Settings -> API)"
SESSION_SECRET="run: openssl rand -hex 32"
